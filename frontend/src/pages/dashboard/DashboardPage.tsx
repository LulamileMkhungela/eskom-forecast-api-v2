import DashboardContent from "../../components/dashboard/DashboardContent";


/** NOT routed in AppRoutes. If mounted, children include MOCK dashboard KPIs. */
const DashboardPage = () => {
  return <DashboardContent />;
};

export default DashboardPage;